# TestWeaver 交付說明（Delivery Notes）

> Web 化測試管理平台：**規格書 → AI 自動生成測試項目**、`Test Plan → Function → Case` 三層管理與版本、自測對應、Fail→缺陷追蹤與自動補件（Revision Request）、完成後**報表 + AI 分析（ECharts）**，並內建 **RAG 智能檢索**與**完整認證層（JWT 登入 + 角色選單 + 稽核日誌 + 帳號管理）**。

**交付狀態：✅ 全部功能與 NFR（認證/稽核/帳號管理/CI/部署）已實作、有測試覆蓋、可 `docker compose up` 跑。**

---

## 1. 完成狀態總表

| 里程碑 | FR | 內容 | 狀態 |
| --- | --- | --- | --- |
| M1 / M7 | FR-1 / 1a | 規格書解析 + AI 生成測試項目（共通類別平台衍生） | ✅ 完成 + 測試 |
| M2 / M3 | FR-2 / 2a | 三層測試結構 + 版本；自測對應（Function+Case 綁定） | ✅ 完成 + 測試 |
| M4 / M5 | FR-3 / 3a | 執行案例 Fail→自動缺陷；無案例缺陷→自動 Revision Request | ✅ 完成 + 測試 |
| M6 | FR-4 | 計畫完成→報表指標（ECharts）+ AI 分析建議 + 一鍵轉 Revision | ✅ 完成 + 測試 |
| M8 | FR-5 | JWT 登入 + 角色控制選單 | ✅ 完成 + 測試 |
| M8+ | FR-5 延伸 | 稽核日誌 + 帳號/角色管理（admin） | ✅ 完成 + 測試 |
| M9 | FR-6 | RAG（索引/檢索/AI 查詢/引用溯源） | ✅ 完成 + 測試 |
| — | FR-5 / NFR | CI（Makefile + GitHub Actions）、Docker 部署 + env 接線 | ✅ 完成 |

**量化**：18 張資料表、41 個 API 路徑、9 個 Alembic 遷移；**後端 144 pytest + 前端 51 vitest**（tsc 0、vite build OK）。

---

## 2. Changelog（依里程碑交付內容）

### M9 — RAG 智能檢索（FR-6）
- `VectorStore` 抽象介面 + Qdrant / pgvector 實作（可切換）；`Embedder` protocol。
- 切塊（chunking）+ 索引（Celery task）+ 檢索（相似度門檻 + top_k）+ AI 查詢 + 引用溯源。
- 模型：`knowledge_chunk`、`rag_query_log`；`source_type` 用 `VARCHAR(16)+CHECK`。
- API：`/rag/index`、`/rag/query`、`/rag/query/async`、`/rag/tasks/{id}`、`/rag/query-logs`。

### M1 / M7 — 規格書 → 測試項目（FR-1 / 1a）
- 專案/規格書上傳解析（`.txt/.md/.pdf/.docx`）；AI 從規格抽出候選測試項目（LLM 優先、離線 heuristics fallback）。
- **FR-1a**：項目必對應共通類別平台類別；無匹配→自動建類別 + 標 `is_newly_created`。
- 模型：`project`、`spec_file`、`test_item_category`、`test_item`、`test_item_platform_sync`。
- Celery task `generate_items_task`；通用任務狀態 `/tasks/{id}`。

### M2 / M3 — 三層結構 + 自測（FR-2 / 2a）
- `Test Plan → Function → Case`，含計畫**版本號 + 狀態機**（draft→in_progress→completed）、功能/案例排序。
- 建計畫可引用 `test_items`（`test_plan_item` 關聯表）；「提升版本」保留結構。
- **FR-2a**：自測必同時綁定 Function + Case，缺一→422。
- 模型：`test_plan`、`test_function`、`test_case`、`test_plan_item`、`self_test`。

### M4 / M5 — 缺陷追蹤 + 自動補件（FR-3 / 3a）
- 執行案例（pass/fail/blocked）；**FR-3**：Fail→自動建**恰好 1** 個 Defect。
- Defect **狀態機**（open→in_progress→fixed→closed 等）+ 指派。
- **FR-3a**：無 `test_case_id` 的 Defect（覆蓋缺口）→ 自動建 **Revision Request**；接受→補件 + 計畫版本+1，拒絕→需理由。
- 模型：`test_execution`、`defect`、`test_plan_revision_request`。

### M6 — 報表 + AI 分析（FR-4）
- 計畫標記完成→計算指標（通過率/缺陷分佈/覆蓋缺口/高風險）+ AI 分析（LLM JSON 優先、離線 fallback）。
- `ai_report` 存 summary + recommendations(JSON) + metrics。
- API：`POST /test-plans/{id}/complete`（**同步生成**，免 worker）、`GET /reports/{id}`、`GET /reports/{id}/export`(CSV)。
- 前端 **ECharts**（案例結果環餅 + 缺陷嚴重度長條）+ AI 建議卡片 + **一鍵轉 Revision Request**（新增 `POST /revision-requests`）。

### M8 — 認證層（FR-5 + 延伸）
- **JWT 登入**（`/auth/login`、`/auth/me`）；HS256 JWT + PBKDF2 密碼**皆用標準函式庫，零新增依賴**。
- **角色控制選單**：`admin`（全）/ `qa_lead`（無 platform、settings）/ `tester`（核心四項）。
- **全域 token 護**：`AUTH_ENABLED=false`（預設）no-op；啟用後 `/auth/*` 以外需 Bearer，`/platform`、`/audit`、`/users` 另需 admin。
- **稽核日誌**（middleware 記錄 POST/PATCH/DELETE + login）：`AUDIT_ENABLED` 預設 false；admin 於 `/audit/logs` 檢視。
- **帳號管理**（admin）：新增/改角色/停用/重設密碼；**最後一個啟用 admin 不可被移除**。
- **JWT refresh**（FR-5 延伸）：access token 短效（預設 60min）+ refresh token 長效（預設 7d，DB 存 `jti`）；`POST /auth/refresh` 旋轉（作廢舊、發新）、`POST /auth/logout` 登出撤銷；前端 401 自動刷新。
- 模型：`user`、`audit_log`、`refresh_token`；首次啟動自動建 admin（`ensure_default_admin`，env 可覆蓋）。

### CI / 部署
- **CI**：`Makefile`（`make ci`）+ GitHub Actions（`.github/workflows/ci.yml`，push/PR 跑 Backend·pytest + Frontend·tsc/vitest/build）。
- **Docker**：`docker-compose.yml`（mysql / redis / qdrant / api / worker），api 啟動先 `alembic upgrade head`；auth/audit env 已接線（`.env` 可覆蓋）。

### 多團隊（v1.1，T1–T3，epic #2）
- **資料層**（T1）：`team` / `team_member`（UNIQUE(team_id,user_id)）；`project.team_id` + migration `0010`（預設團隊 backfill、role 映射 admin→owner）；專案資料隔離（跨團隊 404 / 建他隊 403，`AUTH off` → permissive）。
- **team CRUD + RBAC**（T2）：`/teams` + `/teams/{id}/members`（加人/改角色/移除）；owner/platform admin 可管理、tester→403、非成員→404；**最後一個 owner 保護→400**。前端 TeamContext + TeamSwitcher + TeamsPage；選單隨 team role。
- **platform admin + 稽核**（T3）：`DELETE /teams/{id}`（僅 platform admin，有專案→409）；`audit_log.team_id` + migration `0011`；platform admin 操作非成員團隊 → 稽核 detail=`admin_override`。
- 模型 +2 表（`team`、`team_member`）、API +10 路徑；後端測試 144→165、前端 51→59。
- 設計/狀態詳見 `docs/規劃-多團隊.md`。

---

## 3. 技術棧 & 架構

| 層 | 技術 |
| --- | --- |
| 前端 | React 18 + TypeScript + Ant Design v5 + **ECharts** + react-router（Vite 5，dev proxy `/api→:8000`） |
| 後端 | Python 3.11 + FastAPI（REST） |
| ORM / DB | SQLAlchemy 2.0 + Alembic；MySQL 8（本機可 SQLite） |
| 任務佇列 | Celery + Redis |
| 向量庫 | Qdrant（預設）/ pgvector（可切換，`VectorStore` 抽象） |
| AI | Embedding + LLM（OpenAI 相容，可切 base url）；離線 heuristics fallback |
| 認證 | JWT（stdlib HS256）+ PBKDF2 密碼（stdlib）；無新增依賴 |

**分層慣例**：API 薄（驗證/HTTP）→ **Service** 放業務規則（domain `Error` 帶 `.status_code`，API catch→HTTPException）→ ORM。狀態機（Plan/Defect/Revision）在 Service 強制，違反→409。

---

## 4. 資料模型（21 表）

- **專案/規格**：`project`、`spec_file`
- **共通類別平台（M7）**：`test_item_category`
- **測試項目（M1）**：`test_item`、`test_item_platform_sync`
- **三層結構（M2）**：`test_plan`、`test_function`、`test_case`、`test_plan_item`
- **自測（M3）**：`self_test`
- **執行/缺陷（M4）**：`test_execution`、`defect`
- **修改要求（M5）**：`test_plan_revision_request`
- **報表/AI（M6）**：`ai_report`
- **認證（M8）**：`user`、`audit_log`
- **RAG（M9）**：`knowledge_chunk`、`rag_query_log`

**遷移鏈（9）**：`0001_rag → 0002_m1 → 0003_m2 → 0004_m3 → 0005_m4 → 0006_m5 → 0007_m6 → 0008_auth_user → 0009_audit_log`

---

## 5. API 面（53 路徑，依模組）

- **認證**：`/auth/login`、`/auth/me`
- **帳號管理（admin）**：`GET/POST /users`、`PATCH /users/{id}`
- **稽核（admin）**：`GET /audit/logs`
- **類別平台（admin）**：`GET/POST /platform/categories`、`GET /platform/categories/{id}`
- **專案/規格/項目（M1）**：`GET/POST /projects`、`/projects/{id}`、`GET/POST /projects/{id}/spec-files`、`.../generate-items`、`GET /projects/{id}/test-items`
- **三層（M2）**：`/projects/{id}/test-plans`、`/test-plans/{id}`(+`/functions`/`/tree`/`/revisions`)、`/functions/{id}/cases`、`/test-functions/{id}`、`/test-cases/{id}`(+`/status`)
- **自測（M3）**：`POST /self-tests`、`GET /projects/{id}/self-tests`
- **執行/缺陷（M4）**：`POST /test-executions`、`GET /test-cases/{id}/executions`、`GET/POST /defects`、`GET/PATCH /defects/{id}`
- **修改要求（M5）**：`GET/POST /revision-requests`、`.../{id}`(+`/accept`/`/reject`/`/complete`)
- **報表（M6）**：`POST /test-plans/{id}/complete`、`GET /reports/{id}`(+`/export`)
- **RAG（M9）**：`/rag/index`、`/rag/query`、`/rag/query/async`、`/rag/tasks/{id}`、`/rag/query-logs`
- **任務狀態**：`GET /tasks/{id}`

---

## 6. 認證與安全

**角色矩陣（選單可見性）**

| 選單 | admin | qa_lead | tester |
| --- | :-: | :-: | :-: |
| RAG / 測試計畫 / 自測 / 缺陷追蹤 | ✅ | ✅ | ✅ |
| 報表 / AI | ✅ | ✅ | — |
| 共通類別平台 | ✅ | — | — |
| 設定（帳號+稽核） | ✅ | — | — |

**安全決策**
- 密碼：PBKDF2-HMAC-SHA256 + 每用戶 salt（不存明文）。
- JWT：HS256、stdlib 實作、`JWT_SECRET`（生產必填）；payload `sub`=user id，解碼後查 DB 取即時 role/active。
- `AUTH_ENABLED` / `AUDIT_ENABLED` **預設 false**：離線開發與既有測試 no-op；啟用才生效。
- **最後一個啟用 admin 不可降級/停用**（防鎖死）。
- 敏感端點雙重護：token + admin 角色。
- 預設帳號 `admin / admin123`（`ensure_default_admin`，`AUTH_ADMIN_USERNAME/PASSWORD` 可覆蓋；**上線前務必改**）。

---

## 7. 測試與 CI

**測試（全部離線可跑：SQLite + monkeypatch，免 Qdrant/Redis/LLM key）**
- 後端 **144**：`test_rag`、`test_m1~m6(+_api)`、`test_auth(+_api)`、`test_audit(+_api)`、`test_users(+_api)`。
- 前端 **51**：`api/m1~m6 / auth / audit / users / client`、`hooks/useTask`、`components/EChart`、`auth/roles`（fetch 以 mock，無後端）。

**如何跑**
```bash
make ci            # 一條指令：typecheck + 後端 pytest + 前端 vitest + build
make test          # 只跑測試；另有 test-backend / test-frontend / typecheck / build
```
**GitHub Actions**：`.github/workflows/ci.yml` — push/PR 跑兩個 job（`Backend·pytest`(Python 3.11) + `Frontend·tsc+vitest+build`(Node 20)），含 pip/npm cache。

---

## 8. 部署（Docker）

`docker-compose.yml`：`mysql` / `redis` / `qdrant` / `api` / `worker`。`api` 啟動先跑 `alembic upgrade head`（冪等）再啟 uvicorn；`worker` 跑 Celery。

**關鍵環境變數（`.env`，compose 已接線、有預設值）**

| 變數 | 預設 | 說明 |
| --- | --- | --- |
| `EMBEDDING_API_KEY` | （必填） | RAG 索引/檢索金鑰 |
| `LLM_API_KEY` / `LLM_BASE_URL` / `LLM_MODEL` | 可選 | AI 答案/分析；未填走離線 fallback |
| `AUTH_ENABLED` | `false` | 啟用 JWT 認證 |
| `JWT_SECRET` | dev placeholder | **生產必填** |
| `AUDIT_ENABLED` | `false` | 開稽核日誌 |
| `AUTH_ADMIN_USERNAME` / `AUTH_ADMIN_PASSWORD` | admin / admin123 | 首次啟動的預設 admin |

---

## 9. 快速上手

**A) Docker（推薦）**
```bash
cp .env.example .env          # 填 EMBEDDING_API_KEY（必填）；要認證就設 AUTH_ENABLED=true
docker compose up --build
# API: http://localhost:8000/docs　　Frontend（另起）: cd frontend && npm install && npm run dev → http://localhost:5173
# 啟用認證時以 admin / admin123 登入
```

**B) 本機（免 Docker）**
```bash
cd backend && pip install -r requirements.txt
python -m pytest tests -q                          # 離線綠燈
alembic upgrade head && uvicorn app.main:app --reload
celery -A app.workers.celery_app.celery_app worker --loglevel=info
cd ../frontend && npm install && npm run dev        # Vite proxy /api → :8000
```

---

## 10. 驗證清單（逐 FR）

| FR | 怎麼驗 |
| --- | --- |
| FR-1/1a | 上傳規格書 →「生成測試項目」→ 產生項目且映射類別（無匹配自動建類別） |
| FR-2/2a | 建計畫（選項目）→ 加功能/案例 → 改狀態/提升版本；自測需同時選 Function+Case |
| FR-3/3a | 執行案例選 Fail → 自動開 1 缺陷；手動建無案例缺陷（帶 plan）→ 自動開 Revision Request → 接受補件 + 版本+1 |
| FR-4 | 計畫標記完成 → `/report` 見 ECharts + AI 建議；點「轉 Revision Request」建立修改要求 |
| FR-5 | 設 `AUTH_ENABLED=true`：未登入→跳 `/login`；admin/qa_lead/tester 選單差異；`AUDIT_ENABLED=true`→`/settings` 見稽核日誌與帳號管理（最後一個 admin 保護） |
| FR-6 | `/rag`：建索引→查詢→AI 查詢→查引用來源與紀錄 |

---

## 11. 已知限制 / 後續建議

- **權限邊界**：v1.1 已加「團隊級 RBAC + 專案資料隔離」（team owner/platform admin、跨團隊 404）；尚未對每個讀取端點細化角色（NFR 的完整 RBAC 可再細化）。
- **Bundle size**：ECharts 全量 import（~2.3MB）；可改 `echarts/core` + 只註冊 bar/pie 組件瘦身。
- **JWT 過期管理**：已有 refresh token（可登出撤銷/旋轉）+ access 短效；尚未做「refresh token 輪轉上限 / 裝置管理」等進階治理。
- **稽核**：目前記 HTTP 層（method/path/status/user）；若需欄位級 before/after diff，可在 Service 層加詳細 `detail`。
- **前端測試**：已覆蓋 API 客戶端 / hook / EChart / roles；頁面級（Storybook/RTL 流程）可再補。
- **覆蓋率**：已加門檻（後端 `pytest-cov` ≥82% / 前端 vitest coverage ratchet）+ CI upload coverage artifact（見 `Makefile` + `.github/workflows/ci.yml`）。
- **生產**：限流、CORS 白名單、HTTPS、`JWT_SECRET` 長隨機、MySQL 連線池參數、備份策略。
- **多團隊截圖**：已補 `shots/teams.png`（header switcher + 成員管理頁）並加入 `一頁摘要.html`；`scripts/capture-shots.sh` 可重拍。

---

*設計細節（NFR、資料庫 ERD、核心流程、API spec）見 [`程式計劃書.md`](./程式計劃書.md)；操作說明與 env 見 [`README.md`](./README.md)。*
